{
  "name": "react-mesto-api-full",
  "version": "0.1.0",
  "private": true,
  "author": "Ilya Sychugov (https://github.com/ilyaSy)",
  "scripts": {
    "start": "react-scripts start",
    "build": "cd frontend/ && react-scripts build",
    "test": "react-scripts test",
    "lint": "npx eslint ./backend",
    "deploy": "cd frontend/ && npm run build && scp -r ./build/* ilya@178.154.230.87:/home/ilya/frontend"
  },
  "workspaces": [
    "backend/*",
    "frontend/*"
  ],
  "main": "./frontend/src/index.js",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^11.2.7",
    "@testing-library/user-event": "^12.8.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "web-vitals": "^1.1.2"
  }
}
